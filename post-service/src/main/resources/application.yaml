server:
  port: 8083
  servlet:
    context-path: /post
app:
  services:
    profile:
      url: http://localhost:8081/profile

spring:
  application:
    name: post-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/post-service?authSource=admin
  
